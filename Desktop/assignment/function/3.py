#Write a Python function to multiply all the numbers in a list.

def multiply(*args):
    total = 1
    for i in mul_list:
        total = total * i
    return total

mul_list = [8, 2, 3, -1, 7]
print(multiply(mul_list))